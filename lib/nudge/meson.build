project('regular-tools',
    'cpp',
    default_options: [
        'cpp_std=c++11',
        'warning_level=3',
        'default_library=static'
    ]
)

nudge_sources = [
    'nudge.cpp', 'nudge.h'
]
library('nudge', nudge_sources)